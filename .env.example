# Root .env — optional, for har-research / scripts experiments only
# NOT used by ./run-local.sh
#
# Demo stack env:
#   vision-ops-app/.env.local     → NEXT_PUBLIC_API_URL=http://localhost:8000
#   vision-ops-backend/.env       → WEBCAM_*, HAR_*, VISIONOPS_*

# Optional: event buffer for scripts/04_build_event_buffer.ipynb (legacy Phase 0)
# REDIS_URL=redis://localhost:6379/0
