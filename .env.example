# Root .env — optional, for notebook experiments only (NOT used by ./run-local.sh)
#
# Demo stack env files:
#   vision-ops-app/.env.local       → NEXT_PUBLIC_* URLs
#   vision-ops-alerting/.env        → ALERTING_* (email, auth, SQLite, Ollama)
#   vision-ops-backend/.env         → CAMERA_*, CORS, face/vision toggles

# Optional: event buffer (notebook 04)
# REDIS_URL=redis://localhost:6379/0
