# VisionOps Fase 0 — copy to .env and fill values (never commit .env)

# Cloud semantics (notebook 05)
GEMINI_API_KEY=

# Alerts (notebook 07)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Optional: event buffer backend (notebook 04) — requires `pip install redis` locally
REDIS_URL=redis://localhost:6379/0

# Optional: JWT / API (Fase 1)
JWT_SECRET=change-me-in-production
